spring:
  profiles:
    default: dev
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

server:
  port: 8000
